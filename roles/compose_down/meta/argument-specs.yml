---
argument_specs:
  main:
    short_description: 'docker compose down'
    description:
      - 'Take down a docker project with compose'
    options:
      compose_down_path:
        type: 'str'
        required: true
        description:
          - 'Path to the compose project to take down'
...
